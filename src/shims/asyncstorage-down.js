/**
 * Stub for asyncstorage-down.
 *
 * This package is abandoned and is only required transitively via
 * react-native-level-fs → asyncstorage-down. Since we never call
 * StoreSession, this code path is dead. Returning a no-op object prevents
 * bundler resolution failures.
 */
module.exports = {};
